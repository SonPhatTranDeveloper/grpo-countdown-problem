# Main configuration file for GRPO training
defaults:
  - dataset: default
  - model: qwen2.5-3b
  - training: default
  - _self_

# Output directory for model checkpoints and logs
output_dir: outputs/grpo-arithmetic-model

# Random seed for reproducibility
seed: 42
